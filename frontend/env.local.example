# ==============================================================================
# Paste Trader - Frontend Environment Configuration Example
# ==============================================================================
# Usage: Copy this file to .env.local and fill in your actual values
#        cp env.local.example .env.local
#
# All NEXT_PUBLIC_ prefixed variables are exposed to the browser
# ==============================================================================

# ------------------------------------------------------------------------------
# API CONFIGURATION
# ------------------------------------------------------------------------------
# Backend API URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# API version prefix
NEXT_PUBLIC_API_VERSION=v1

# WebSocket URL for real-time updates (optional)
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# ------------------------------------------------------------------------------
# FEATURE FLAGS
# ------------------------------------------------------------------------------
# Enable/disable features for development
NEXT_PUBLIC_ENABLE_DEVTOOLS=true
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_MOCK_DATA=false

# ------------------------------------------------------------------------------
# UI CONFIGURATION
# ------------------------------------------------------------------------------
# Default theme: light, dark, system
NEXT_PUBLIC_DEFAULT_THEME=system

# Default language: en, ko
NEXT_PUBLIC_DEFAULT_LOCALE=ko

# ------------------------------------------------------------------------------
# EXTERNAL SERVICES (Browser-safe only)
# ------------------------------------------------------------------------------
# Analytics tracking ID (if using)
NEXT_PUBLIC_ANALYTICS_ID=

# Sentry DSN for frontend error tracking (optional)
NEXT_PUBLIC_SENTRY_DSN=

# ------------------------------------------------------------------------------
# BUILD CONFIGURATION
# ------------------------------------------------------------------------------
# Build-time environment indicator
NEXT_PUBLIC_BUILD_ENV=development

# Application version (typically set in CI/CD)
NEXT_PUBLIC_APP_VERSION=0.1.0
